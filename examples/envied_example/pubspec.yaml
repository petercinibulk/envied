name: example
description: A sample command-line application.
version: 2.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  build_runner: ^2.4.6
  envied_generator: ^0.5.2
  lints: ^3.0.0
  test: ^1.24.9
dependencies:
  envied: ^0.5.2
